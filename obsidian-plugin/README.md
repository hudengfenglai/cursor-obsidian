# Cursor Sidecar (Obsidian plugin) v0.6.0

Context Follow keeps Cursor on the active Obsidian file (silent, no focus steal) while attached.
Packaged installs use `bin/cursor-sidecar.exe`; Developer mode can fall back to `python main.py`.
